# Aade invoice creator

golang implemented commandline that generates an invoice in AADE gov gr system.

```
./bin/aade-invoice <amount>
```

It should register a new invoice in aade system.
It should download the invoice and print it as PDF.

